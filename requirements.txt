torch==2.9.1
torchvision==0.16.0
torchaudio==2.1.0

# 自然语言处理/多模态特征提取
transformers==4.36.2  # 用于DistilBERT文本编码
datasets==2.15.0      # 加载Amazon_Books数据集

# 数据处理核心库
numpy==1.26.2         # 数值计算（避免高版本兼容性问题）
pandas==2.1.4         # 数据清洗/存储
scikit-learn==1.3.2   # 评估指标（NDCG/MRR）/数据划分

# 可视化库
matplotlib==3.8.2     # 基础绘图
seaborn==0.13.0       # 热力图/柱状图

# 工具类库
tqdm==4.66.1          # 进度条显示
pickle-mixin==1.0.2   # 兼容pickle存储（可选，Python内置pickle也可）
psutil==5.9.6         # 监控GPU/内存使用（可选，便于调试）