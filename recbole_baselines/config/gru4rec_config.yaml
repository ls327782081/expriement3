# GRU4Rec 模型配置
# Session-based Recommendations with Recurrent Neural Networks (ICLR 2016)

# 模型参数
embedding_size: 64
hidden_size: 128
num_layers: 1
dropout_prob: 0.3

# 损失函数
loss_type: 'CE'  # Cross Entropy

